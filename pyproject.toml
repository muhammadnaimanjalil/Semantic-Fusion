[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"
pythonpath = ["."]

[tool.ruff]
target-version = "py312"
line-length = 88
exclude = [".venv", ".git", ".codex-paper-review"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
